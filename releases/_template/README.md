# Release candidate

`freeze-release` creates `releases/rcNNN/` with a sealed RC record, gate,
checksums, and a prepared GitHub Release asset manifest. The staged binary is
gitignored and is not a second long-term canonical copy.
