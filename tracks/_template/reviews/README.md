# Reviews

Record blind listening before unblinding provider inputs. The sealed review
TOML is the canonical human gate record. `listening.csv` is rebuilt from those
records as a convenience view; a failed rebuild may leave it stale without
invalidating an already committed review.
